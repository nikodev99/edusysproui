import {InfoPageProps} from "../../../utils/interfaces.ts";
import {Classe} from "../../../entity";

export const ClasseExams = ({infoData}: InfoPageProps<Classe>) => {
    return(
        <div>Classe Exams</div>
    )
}