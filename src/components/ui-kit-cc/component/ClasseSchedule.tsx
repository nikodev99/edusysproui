import {InfoPageProps} from "../../../utils/interfaces.ts";
import {Classe} from "../../../entity";

export const ClasseSchedule = ({infoData}: InfoPageProps<Classe>) => {
    return (
        <div>Classe Schedule</div>
    )
}