import {ActionLinksProps, ActionsButtons} from "../../../core/utils/interfaces.ts";

type UserActionButtons = ActionsButtons

export const UserActionLinks = ({open, setActions, show}: ActionLinksProps<UserActionButtons>) => {
    return(
        <div>

        </div>
    )
}